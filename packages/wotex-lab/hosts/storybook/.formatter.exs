[
  import_deps: [:phoenix, :phoenix_live_view, :phoenix_storybook],
  plugins: [Phoenix.LiveView.HTMLFormatter],
  inputs: ["{mix,.formatter}.exs", "{config,lib,storybook,test}/**/*.{ex,exs,heex}"]
]
