{
  "skip_files": [
    "test/support/.*",
    "lib/mix/tasks/.*",
    "lib/wotex/lab/docs/.*"
  ],
  "skip_file_reasons": {
    "test/support/.*": "Test-only fixtures are not production coverage surfaces.",
    "lib/mix/tasks/.*": "Mix tasks are command wrappers qualified by their owning package gates.",
    "lib/wotex/lab/docs/.*": "Documentation producers are qualified through the Workbench documentation host gate."
  },
  "coverage_options": {"minimum_coverage": 95}
}
