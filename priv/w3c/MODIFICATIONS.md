# Modification notice

The bundled `td-json-schema-validation-1.1.json` is copied from the W3C WoT
Thing Description repository at tag `REC1.1`, commit
`7c0b968f403ecdb9594bd882cafbacf544c41fc0`.

The JSON content is unchanged. The filename was changed to include the target
specification version, and this companion notice and package provenance record
were added for redistribution. The schema remains informative; Wotex adds
separate semantic checks and does not present the schema as the complete
normative definition of Thing Description 1.1.

The bundled `tm-json-schema-validation-1.1.json` is copied from the same tag,
commit, and upstream repository. Its filename was changed to include the
target specification version and a final line-feed byte was added. No JSON
value was changed. The exact upstream and bundled digests are recorded in
`docs/provenance/w3c-tm-schema-1.1.md`.
